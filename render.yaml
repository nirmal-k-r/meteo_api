services:
  - type: web
    name: meteo-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    healthCheckPath: /health
